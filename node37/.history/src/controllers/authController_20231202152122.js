export const login = () => {

}

export const signUp = () => {
    try {
        let { fullName, email, password} = res.bpdy;
        //thêm mới users => creates
    } catch {
        responseData(res, "Lỗi ...", 500);
    }
}