import respm

export const login = () => {

}

export const signUp = () => {
    try {
        let { fullName, email, password} = res.body;
        //thêm mới users => creates
    } catch {
        responseData(res, "Lỗi ...", 500);
    }
}